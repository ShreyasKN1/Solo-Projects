LLM important features:

RAG Chunking:

Building AI Rag,
Bad chunking  =>  bad embeddings => irrelevant retrieval  =>  hallucination
1)Fixed chunking{ what i know with using spring ai-> etl pipelines-> there is also few parameters in documentreaders{parameters} and transformers for example tokentextsplitters.trasnform( 
we can provide parameters like-> defaultChunkSize, minCharChunk, minLengthtoEmbed, maxnumchunk{how much per document chunks including page numbers] and keepSeparator(go through what parameters what each do and how documents are read in chunks,
cons:if we stick to fixed chunking there is no guarantee that the words will have same meanings, semantics and word meaning changes if we have fixed length of chunks retrieved.
2) there is huge chunks maybe with help of context window that we can create but at the end of the day meaning and similarity will be lost again and system might not able to handle large embeddings and their meanings.
3)then there is Parent-Child Sliding Window chunking with overlap-> with help of semantics meaning here we might have a bit help of embeddings and their meanings then chunks pulled will have slight meaning because we include parent semantics too here in child semantics with overlap and it will remember what the last words were so better meanings will be pulled.
4)Structure-aware chunks-> imagine a html page -> they form chunks based on tags-> section will be separated by other p tag or something, see if we do chunking this way sections will have it's own semantic meaning right this better we can separate chunks and keep their importance together.
5)Sematic chunking + preposition -> no token/headers required and considered best with preposition[self contained topics] + best of best ways of chunking using similaritysearch or even our cutoff ones for[probability like 0.7 then semantics or words/letters with probability less than 0.7 won't be considered in this chunking], similarity search u provide threshold and top k. that with self contained topics which have same meanings are together put into chunks.
6) Retrieval-Aware Chunking -> Chunking should not be done in isolation & align with topk retrieval, similarity thresholds and their embedding behavior
Example->Small chunks -> need higher topk and large chunks -> need stricter similarity threshold
7) Metadata Enrichment->Each chunk will stored as below
Source
Section
Title
Timestamp
Parent reference 
This is very important for legal system documents or law documents or documents with table
then we can do search query+keyword filtering+vector rerank ->llm
8)Re-ranking Layer->Add re-ranking model and sort chunks by relevance post-retrieval
Even with good chunking but Initial retrieval may not be perfect so this method can be used

Hallucination reducing stratgies:

1) Temperature control: when we think about various llms we also have to think how the temperature affects in every llm[ as I have seen some with range 0-1.0] and some with [0-2.0 and 1.0 kept balanced] => less temperature means determinstic output and more temperature hallucinate probability is more and output is varied sometimes it increases randomness.
2) Confidence caliberation and abstention policy:
	-> there is few metrics we can use here for reducing hallucinate is we try to find relevant words and cosine similarity of the embeddings then we use a formula which is w(cosine similarity)+w1(relevant words)+w2... =1.0 set weights accordingly and this here formula helps us reduce hallucinations since embeddings behaviour also considered for calculation.
	abstention policy: In AI systems with respect to how context is returned we block ifit goes outside context and provide output as “I don’t have enough information”
“Not found in provided context”.
3) Citations chain: we ask llm a question but also expect it to provide source of truth either doccument, data metrics, pie chart for the output with this you can fgure out how the llm is providing response to the required inputs.
4) Self sampling consistency: Generate multiple answers based on context
Select either:
Most frequent answer 
Highest confidence answer
5) Even advanced approaches would be guardrails-> providing boundaries what to avoid and pros and cons for the advice suggested by llms for like investing like stock or etc.
6) prompt engineering helps reduce hallucinations with help of few shots prompting/ system prompts/ chain of thoughts[maybe little hallucinaiton since decision making is important here] and advanced features like chain of thoughts reduces hallucination of response from llm
7) RAG  -> user query=> get document=> inject context=> llms, so that LLM  doesn’t rely on memory & they use external verified data

Additional with formula add one service layer that can be done post retrieval-> groundness, retrieval and explanation quality that can be set and compared and send if metrics are not as per your needs back to llm to get better output.
