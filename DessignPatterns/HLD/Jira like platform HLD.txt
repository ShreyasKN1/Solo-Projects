Jira like platform HLD:
Questions before:
What is the basic unit?
	ticket or issue
Do we have projects?
do we have users?
do we have teams?
do we have roles?
Add comments?
Add attachmenets?
add labels?
set priorities?
status workflows?
track issue history?
seach Indexer and notification.


=>Functional requirments:
Create project
create Issue
Assign Issue
status update
Add comment
Add label
set priorities
search/filter issues
track issue history
notification

=>Non-functional Requirements:
Highly available
Scalable
fast issue retrieval
secure authorisation
consistent updates
durable data
auditability




=>APIs:
project-> /project
project id-> get -> /project/{project_id}
issues-> post->/project/{project_id}/issues
issues-> get /issues/{issues_id}

update issue-> patch -> /issue/{issue_id}
Add comments-> /issue/{issueId}/comments
search API important -> /issue?project_id=101&Status=Open&assigne_id=123


=>Flow:
		APIgateway/LB
		    |
		Jira like Service fr APIs
	        |                 |
		PostgreSQL        Redis
                |
		|
		Kafka
	 _________|____________________________
	| 	              |                |
Notification Service         Auditing Service  Search Indexer[Elasticsearch/OpenSearch]

postgreSQL-> source of truth since expecting durable data + scalable

Redis-> stores user information/ project metadata and frequently accessed issues

Elasticsearch/OpenSearch advanced search pattern we will come to that later


=>Database design:
user-> user_id, name,email,created_at
project-> project_id,name,key,owner_id,created_at

example project_id:101
	key="Pay"
	name="payment platform"
	 then issue_id would be Pay-1234
	
issues:
issue_id
project_id
issue_key
title
description
status
priorities
assignee_id
reporter_id
created_at
updated_at

index:
project_id
(project_id,status)
(assignee_id)


comments:
comment_id,user_id,issue_id,content and created_at

issue history:
history_id,
issue_id
changed_by
filed
old_value
new value
created_at

=>Kafka-> create events: issueCreated
			issueUpdated
			issueAssigned
			Comment added
			Status updated
  events drive:notifications,search indexer, auditing and analytics



=>Issue lifecycle:
open->inprogress->code review->testing->done

so basically don't hardcode status
workflow-> status
	->transitions
like example open->inprogress
		inprogress->code review
		 code review->testing..
diff projects can have different workflow.

=>Scenarios:2 users update the same issue at same time-> use versionId versio=5 userA-> priority"High" and userB->priority"Low"
	run -> update issue set priority=High,version=6 where issue_id=101 AND version=5;
		userB tries where version=5 already changed 409 conflicts

=>SearchAPI:
project_id,key,status and assignee_id

where project_id="101"
AND status!="DONE"
AND key="Pay"
AND text contains("payment")
AND assigned to team X

use elastic search/open search for optimized search -> issueAPI->postgreSQL->issue updated->kafka->searchIndexer->ElasticSearch/openSearch 
remember-> db[source of truth] this elastic search is derived search index


=>Failure scenarios:
when kafka problem

-> resolve using outbox pattern ->issue updated in db-> db transaction-> outbox pattern->kafka-> 3 services[noti,searchIndexer,audit service]
in db transaction-> issue updated-> outbox table-> outbox publisher publishes event to kafka

=>Tradeoffs and Scaling:
-> heavy read workload-> use redis-> API Gateway -> Redis-> cache miss-> db[postgreSQL].

-> Large issue history-> partition or archieve old history
->Kafka allows independent consumptions for 3 services
->for secure authorisation-> manage diff project_level permissions[admin,developer,project_manager,viewer]
  think like instead is user loggedIn? is User allowed to access these projects and issues.



